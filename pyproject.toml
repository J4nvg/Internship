[project]
name = "SDSS"
version = "0.1.0"
description = "Simple drone swarm simulator in adversarial hide and seek environment"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.6",
    "networkx>=3.5",
    "numpy==2.3.3",
    "scipy>=1.16.1",
    "seaborn>=0.13.2",
    "tqdm>=4.67.1",
]
